#!/usr/bin/env groovy

// imports the shared library named "kubernetes" and uses the tagged version "v0.6"
@Library('kubernetes@china-pipeline') _

// calls the build using the configuration found in pipeline.yaml
kubernetesPipeline()